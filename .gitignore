# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (NUNCA subir secretos reales)
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# typescript
*.tsbuildinfo

# snapshots experimentales (raw 365Scores no se versiona).
# Los snapshots normalizados se pueden versionar manualmente si se desea.
/data/snapshots/365scores/raw/
/data/snapshots/365scores/cache/

# editor
.vscode/
.idea/

# Capturas de referencia (no parte de la app)
/Play do it/

# Estadisticas mundial (screenshots de referencia, no parte de la app)
/estadisticas mundial/
/Estad*.zip
/estadisticas jugadores/
/Estadisticas*.zip

# Cache grande regenerable: resultados historicos de martj42 (~8MB). Se baja con
# `npm run wc:calibrate`; no se versiona. Los ratings calibrados sí (elo-ratings.json).
/data/worldcup-2026/international-results.json
